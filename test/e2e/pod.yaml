apiVersion: v1
kind: Pod
metadata:
  name: podsample-integration
  labels:
    name: podsample-integration
spec:
  containers:
  - name: podsample-integration
    image: ubuntu
# sleep forever 
    command: ["/bin/bash", "-c", "--"]
    args: ["apt update -y && apt install iputils-ping -y && ping 8.8.8.8"]
---
apiVersion: v1
kind: Pod
metadata:
  name: podsample
  labels:
    name: podsample
spec:
  containers:
  - name: podsample
    image: nginx   
